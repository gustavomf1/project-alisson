name: CD - Deploy Production

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🔑 Configure SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
        chmod 600 ~/.ssh/deploy_key
        ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

    - name: 📋 Copy docker-compose to VPS
      run: |
        scp -i ~/.ssh/deploy_key docker-compose.yml \
        ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }}:/opt/sistema_av_iii/

    - name: 🗄️ Backup MySQL Database
      run: |
        ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} << 'EOF'
          cd /opt/sistema_av_iii

          mkdir -p backups
          if docker ps | grep -q db; then
            echo "📦 Creating database backup..."
            BACKUP_FILE="backups/backup_$(date +%Y%m%d_%H%M%S).sql"
            docker-compose exec -T db mysqldump -u root -prootpass sistema_av_iii > $BACKUP_FILE
            echo "✅ Backup created: $BACKUP_FILE"
            
            # Keep only last 7 backups
            ls -t backups/backup_*.sql | tail -n +8 | xargs -r rm
          else
            echo "ℹ️ No running DB container to backup"
          fi
        EOF

    - name: 🔄 Deploy application
      run: |
        ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} << 'EOF'
          cd /opt/sistema_av_iii

          echo "🔄 Stopping old containers..."
          docker-compose down --remove-orphans

          echo "🔄 Building and starting new containers..."
          docker-compose up -d --build

          echo "✅ Containers started"

          echo "🔄 Running database migrations..."
          # Ajuste o comando se for outro tipo de aplicação
          docker-compose exec -T app php artisan migrate || true

          echo "✅ Migrations completed"

          echo "🏥 Health check..."
          sleep 10
          MAX_RETRIES=10
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if curl -f http://localhost:8080/health > /dev/null 2>&1; then
              echo "✅ Health check passed!"
              exit 0
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "⏳ Health check attempt $RETRY_COUNT/$MAX_RETRIES failed, retrying in 5s..."
            sleep 5
          done
          echo "❌ Health check failed!"
          docker-compose logs app
          exit 1
        EOF

    - name: 🧹 Cleanup SSH
      if: always()
      run: rm -f ~/.ssh/deploy_key

    - name: ✅ Deployment Success
      if: success()
      run: |
        echo "🎉 =================================="
        echo "🎉 DEPLOYMENT SUCCESSFUL!"
        echo "🎉 =================================="
        echo "✅ Application deployed to: ${{ secrets.VPS_HOST }}"
        echo "✅ Branch: ${{ github.ref_name }}"
        echo "🔗 Access your application at: http://${{ secrets.VPS_HOST }}:8080"

    - name: ❌ Deployment Failed
      if: failure()
      run: |
        echo "❌ =================================="
        echo "❌ DEPLOYMENT FAILED!"
        echo "❌ =================================="
        echo "Check the logs above for details."
